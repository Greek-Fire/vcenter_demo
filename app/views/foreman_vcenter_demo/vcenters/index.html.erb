<% title _("Vcenters") %>
<% title_actions (display_link_if_authorized(_("Add Vcenter"),  new_vcenter_path, :class => "btn btn-primary")) %>

<%= render 'list' %>

<%= will_paginate_with_info @vcenters %>
